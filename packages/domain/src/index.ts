// @openmdim/domain — domain model and bounded contexts.
// Empty until WU-1+. The Prisma schema (@openmdim/db) is the source of truth for
// the data model; domain types are layered on top here.
export {};
