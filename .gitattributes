# Normalize line endings: store text as LF in the repo, regardless of OS.
* text=auto eol=lf

# Lockfiles & configs stay LF
pnpm-lock.yaml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Binary assets (don't touch)
*.docx binary
*.png binary
*.jpg binary
*.ico binary
